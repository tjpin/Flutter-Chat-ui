class Chat {
  String username;
  String message;
  String profilePic;
  DateTime time;
  bool isRead;

  Chat({
    this.username,
    this.message,
    this.profilePic,
    this.time,
    this.isRead
  });
}